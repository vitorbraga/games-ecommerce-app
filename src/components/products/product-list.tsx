import * as React from 'react';

// import styles from './product-list.module.scss';

export class ProductList extends React.PureComponent<{}, never> {
    public render() {
        return (
            <div>ProductList</div>
        );
    }
}
